apiVersion: v1
kind: Service
metadata:
  name: compressor
  namespace: digital-library
  labels:
    app: compressor
spec:
  type: ClusterIP
  selector:
    app: compressor
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
