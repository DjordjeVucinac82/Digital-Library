apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: digital-library
  labels:
    app: worker
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
